version: 2


exposures:

  - name: weekly_metrics
    label: Arif by the Week
    type: dashboard
    maturity: high
    description: 
     For more exposure about project details

    depends_on:
      - ref('customers')
      - ref('final_bus')
      - ref('order_append')
      - ref('order_insert_overwrite')
      - ref('order_merge')
      - ref('Orders')
      - ref('employees_salary')
      - ref('seed_data')
      - ref('snap_customers_timestamp')
      - ref('snap_orders_check')

      - source('datafeed_shared_schema','RAW_ORDERS')
      - source('datafeed_shared_schema','RAW_CUSTOMER')
      - source('datafeed_shared_schema','RAW_PRODUCTS')
      - source('datafeed_shared_schema','EMP_SAL')
  
    owner:
      name:  Arif khan
      email: 786arifkhan0@gmail.com